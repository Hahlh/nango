---
title: 'Perform workflows with an API'
sidebarTitle: 'Perform workflows with an API'
description: 'Step-by-step guide to performing workflows with an API (using _actions_).'
---

<Info>
Pre-Requisite: You should have already created an [integration](TODO) and at least one [connection](TODO) (cf. [Authorize An API](TODO) step-by-step guide).
</Info>

# Activate an action template

Nango uses [actions](TODO) to perform workflows involving external APIs. Workflows can involve arbitrary series of API requests & data transformations. For common use cases, [templates](TODO) are available to let you get started fast.

Select your integration in the _Integrations_ tab, and navigate to the _Scripts_ tab. Available action templates will appear in the _Action Scripts_ section. Select the relevant one and enable it with the toggle.

<Tip>
Is there no action template matching your exact use case? Learn more about how to [build a custom integration](TODO), [extend a template](TODO) or [request custom integrations from Nango experts](TODO).
</Tip>

# Trigger an action

Actions can take inputs, and they return a result synchronously. Trigger actions using the [backend SDK](TODO:ref) or [API](TODO:ref). If using the API, you can use a standard endpoint (documented below) or a unique generated endpoint described in the _API Reference_ tab of your integration.

<Tabs>

<Tab title="cURL (standard endpoint)">

```bash
curl --request POST \
  --url https://api.nango.dev/action/trigger \
  --header 'Authorization: Bearer <PROJECT-SECRET-KEY>' \
  --header 'Connection-Id: <string>' \
  --header 'Provider-Config-Key: <string>' \
  --data '{ "action_name": "<string>", "input": <json> }'
```
</Tab>

<Tab title="Node SDK">

```ts
import { Nango }  from '@nangohq/node';

const nango = new Nango({ secretKey: '<PROJECT-SECRET-KEY>' });

const result = await nango.triggerAction('<INTEGRATION-ID>', '<CONNECTION-ID>', '<ACTION-NAME>', jsonInput);
```
</Tab>

</Tabs>

TODO: show action response schema (success/error).


# Troubleshoot errors & monitor

Navigate to the _Activity_ tab to inspect potential errors & monitor action executions. 


<Tip>
**Questions, problems, feedback?** Please reach out on the [Slack community](https://nango.dev/slack).
</Tip>